pipeline {
    agent any
    stages {
        stage('Install ansible roles') {
            steps {
               sh 'echo "pipeline code via github on jenkins"'
            }
        }
    
    }
   
}
